# py-lint-driven Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build a Claude Code marketplace plugin that enforces linter-driven and test-driven development for Python projects automatically.

**Architecture:** Skill-first composable plugin — 7 skills are atomic units, 5 commands compose them into workflows, 1 agent handles multi-step reasoning, 2 hooks trigger automatically on every Python file write/edit. All tool invocations go through Taskfile tasks, never directly.

**Tech Stack:** Claude Code plugin system (plugin.json, SKILL.md, .md commands, .json hooks), Taskfile v3, ruff, complexipy, radon, pytest

---

## Background: Claude Code Plugin Concepts

Before starting, understand these primitives:

- **SKILL.md** — a markdown file that teaches Claude a capability. Claude reads it and knows how to perform the skill.
- **Command (.md)** — a slash command the user invokes (e.g. `/tdd`). Has YAML frontmatter + instructions.
- **Agent (.md)** — a subagent with a system prompt. Launched via the Task tool for multi-step reasoning.
- **Hook (.json)** — fires automatically on events (PostToolUse, SessionStart, etc.). Runs a bash command or prompt.
- **plugin.json** — manifest that declares all components and their paths.
- **py-lint-driven.local.md** — user config file with YAML frontmatter. Claude reads it at the start of each session.

---

## Prerequisites

Before Task 1, install required tools:

```bash
pip install ruff complexipy radon pytest
brew install go-task
```

Verify:
```bash
ruff --version        # ruff 0.x.x
complexipy --version  # complexipy x.x.x
radon --version       # radon x.x.x
pytest --version      # pytest x.x.x
task --version        # Task x.x.x
```

---

## Task 1: Plugin Scaffold

**Files:**
- Create: `py-lint-driven/plugin.json`
- Create: `py-lint-driven/skills/` (empty dir)
- Create: `py-lint-driven/commands/` (empty dir)
- Create: `py-lint-driven/agents/` (empty dir)
- Create: `py-lint-driven/hooks/` (empty dir)
- Create: `py-lint-driven/templates/` (empty dir)

**Step 1: Create the directory structure**

```bash
mkdir -p py-lint-driven/{skills,commands,agents,hooks,templates}
mkdir -p py-lint-driven/skills/{run-ruff,run-complexipy,run-radon,run-pytest,write-tests,iterate-until-clean,report-quality}
```

**Step 2: Create plugin.json**

Create `py-lint-driven/plugin.json`:

```json
{
  "name": "py-lint-driven",
  "version": "1.0.0",
  "description": "Linter-driven and test-driven development for Python. Enforces ruff, complexipy, radon, and pytest automatically.",
  "skills": [
    "skills/run-ruff/SKILL.md",
    "skills/run-complexipy/SKILL.md",
    "skills/run-radon/SKILL.md",
    "skills/run-pytest/SKILL.md",
    "skills/write-tests/SKILL.md",
    "skills/iterate-until-clean/SKILL.md",
    "skills/report-quality/SKILL.md"
  ],
  "commands": [
    "commands/tdd.md",
    "commands/tdd-test.md",
    "commands/lint-fix.md",
    "commands/lint-check.md",
    "commands/quality-report.md"
  ],
  "agents": [
    "agents/lint-iterator.md"
  ],
  "hooks": [
    "hooks/post-write-quality.json",
    "hooks/post-edit-quality.json"
  ]
}
```

**Step 3: Verify structure**

```bash
find py-lint-driven -type f | sort
```

Expected output:
```
py-lint-driven/plugin.json
```

**Step 4: Commit**

```bash
git add py-lint-driven/
git commit -m "feat: scaffold py-lint-driven plugin structure"
```

---

## Task 2: Taskfile Templates

These files live in the user's project, not the plugin. The plugin ships them as templates and generates them on first run.

**Files:**
- Create: `py-lint-driven/templates/Taskfile.yaml`
- Create: `py-lint-driven/templates/Taskfile.python.yaml`

**Step 1: Create `py-lint-driven/templates/Taskfile.yaml`**

```yaml
version: '3'

includes:
  python: ./taskfiles/Taskfile.python.yaml
```

**Step 2: Create `py-lint-driven/templates/Taskfile.python.yaml`**

```yaml
# taskfiles/Taskfile.python.yaml
# Generated by py-lint-driven plugin. Safe to modify — plugin will not overwrite.
# Tasks have no 'python:' prefix — the includes directive in Taskfile.yaml adds it.
# Invoke as: task python:lint, task python:test, etc.
# Within this file, reference sibling tasks by short name (e.g. task: lint, task: ruff).
version: '3'

vars:
  MAX_COGNITIVE_COMPLEXITY: "15"
  RADON_CC_MIN_GRADE: "C"

tasks:

  # --- Leaf tasks: direct tool invocations ---

  ruff:
    desc: "Run ruff check (config auto-discovered from pyproject.toml or ruff.toml)"
    cmds:
      - ruff check {{.FILES | default "."}}

  ruff:fix:
    desc: "Run ruff check with auto-fix"
    cmds:
      - ruff check --fix {{.FILES | default "."}}

  complexity:
    desc: "Run complexipy — exits non-zero if cognitive complexity exceeds MAX_COGNITIVE_COMPLEXITY"
    cmds:
      - complexipy -C {{.MAX_COGNITIVE_COMPLEXITY}} {{.FILES | default "."}}

  maintainability:
    desc: "Run radon cc and mi — always exits 0, output parsed by run-radon skill"
    cmds:
      - radon cc -n {{.RADON_CC_MIN_GRADE}} {{.FILES | default "."}}
      - radon mi {{.FILES | default "."}}

  fmt:
    desc: "Run ruff format"
    cmds:
      - ruff format {{.FILES | default "."}}

  fmt:check:
    desc: "Run ruff format check (no changes applied)"
    cmds:
      - ruff format --check {{.FILES | default "."}}

  test:
    desc: "Run pytest on the tests/ directory"
    cmds:
      - pytest tests/ -v

  test:file:
    desc: "Run pytest on a specific test file. Pass FILE=path/to/test.py"
    cmds:
      - pytest {{.FILE}} -v

  test:watch:
    desc: "Run pytest in watch mode (local use only, requires pytest-watch)"
    cmds:
      - ptw tests/

  # --- Composite tasks ---

  lint:fast:
    desc: "Ruff only — fast, used by hooks when hooks_run_complexity is false"
    cmds:
      - task: ruff
      - echo "--> Ruff check passed"

  lint:
    desc: "Full lint: ruff + complexity + maintainability"
    cmds:
      - task: ruff
      - task: complexity
      - task: maintainability
      - echo "--> All Python linters passed"

  lint:fix:
    desc: "Ruff auto-fix + complexity check + maintainability check (only ruff is auto-fixable)"
    cmds:
      - task: ruff:fix
      - task: complexity
      - task: maintainability
      - echo "--> Ruff auto-fix applied, linters checked"

  check:
    desc: "Full check: lint + format check (read-only)"
    cmds:
      - task: lint
      - task: fmt:check
      - echo "--> All Python checks passed"

  fix:
    desc: "Auto-fix and format"
    cmds:
      - task: lint:fix
      - task: fmt
      - echo "--> Auto-fix and format complete"

  tdd:fast:
    desc: "Tests + ruff only — fast hook mode"
    cmds:
      - task: test
      - task: lint:fast
      - echo "--> Tests passed and ruff clean"

  tdd:
    desc: "Full TDD cycle: tests + ruff + complexity + maintainability"
    cmds:
      - task: test
      - task: lint
      - echo "--> All tests passed and linting clean"

  tdd:fix:
    desc: "Full TDD fix cycle: tests + ruff:fix + complexity + maintainability + fmt"
    cmds:
      - task: test
      - task: lint:fix
      - task: fmt
      - echo "--> TDD cycle complete with fixes applied"

  ci:
    desc: "CI: run checks on changed Python files only. Skips cleanly if none changed."
    vars:
      CHANGED_PY_FILES:
        sh: "git fetch origin ${GITHUB_BASE_REF:-main} --quiet && git diff --name-only --diff-filter=ACMRT origin/${GITHUB_BASE_REF:-main}...HEAD -- '*.py' | tr '\n' ' '"
    # status exits 0 when no changed files → Taskfile skips all cmds cleanly
    status:
      - '[ -z "{{.CHANGED_PY_FILES}}" ]'
    cmds:
      - task: ruff
        vars: {FILES: "{{.CHANGED_PY_FILES}}"}
      - task: complexity
        vars: {FILES: "{{.CHANGED_PY_FILES}}"}
      - task: maintainability
        vars: {FILES: "{{.CHANGED_PY_FILES}}"}
      - task: test
      - echo "--> CI checks passed"
```

**Step 3: Test the Taskfile manually**

Copy templates to a test project and verify each task runs:

```bash
mkdir -p /tmp/test-project/taskfiles /tmp/test-project/tests
cp py-lint-driven/templates/Taskfile.yaml /tmp/test-project/
cp py-lint-driven/templates/Taskfile.python.yaml /tmp/test-project/taskfiles/
cd /tmp/test-project

# Create a minimal clean Python file
echo "def add(a: int, b: int) -> int:\n    return a + b" > main.py

# Create a minimal passing test
cat > tests/test_main.py << 'EOF'
from main import add

def test_add():
    assert add(1, 2) == 3
EOF

task python:ruff      # Expected: exit 0, no violations
task python:test      # Expected: 1 passed
task python:tdd       # Expected: all green
task python:ci        # Expected: skips (no git, or no changed files)
```

**Step 4: Commit**

```bash
cd -  # back to plugin root
git add py-lint-driven/templates/
git commit -m "feat: add Taskfile templates for user projects"
```

---

## Task 3: Configuration Template

**Files:**
- Create: `py-lint-driven/templates/py-lint-driven.local.md`

**Step 1: Create the config template**

```markdown
---
max_cognitive_complexity: 15
radon_cc_min_grade: C
radon_mi_floor: 20
iteration_limit: 5
hooks_enabled: true
hooks_run_complexity: false
tdd_enabled: true
---

# py-lint-driven Configuration

Edit the YAML frontmatter above to configure thresholds and behavior.

## Settings

- `max_cognitive_complexity` — functions exceeding this score are flagged by complexipy (default: 15)
- `radon_cc_min_grade` — cyclomatic complexity grade cutoff: A=simple, F=very complex (default: C)
- `radon_mi_floor` — maintainability index floor 0-100, below this is flagged (default: 20)
- `iteration_limit` — max fix/verify cycles before giving up (default: 5)
- `hooks_enabled` — set to false to disable automatic linting on file write/edit (default: true)
- `hooks_run_complexity` — set to true to run complexipy+radon on every file write (slower, default: false)
- `tdd_enabled` — set to false to revert hooks to lint-only, no test running (default: true)

## Taskfile Thresholds

The vars MAX_COGNITIVE_COMPLEXITY and RADON_CC_MIN_GRADE in taskfiles/Taskfile.python.yaml
should match the values above. They are set separately — keep them in sync manually.
```

**Step 2: Commit**

```bash
git add py-lint-driven/templates/py-lint-driven.local.md
git commit -m "feat: add configuration template"
```

---

## Task 4: run-ruff Skill

**Files:**
- Create: `py-lint-driven/skills/run-ruff/SKILL.md`

**Step 1: Create the skill**

```markdown
---
name: run-ruff
description: Run ruff linter and formatter on Python files. Returns structured violations with file, line, rule code, and message. Supports check mode and auto-fix mode.
triggers:
  - "run ruff"
  - "check ruff violations"
  - "ruff lint"
---

# run-ruff Skill

Run ruff on a Python file or directory and return structured results.

## How to Use

Call `task python:ruff FILES=<path>` (or `task python:ruff` for the whole project).

For auto-fix mode, call `task python:ruff:fix FILES=<path>`.

## Parsing Output

Ruff outputs violations in this format:
```
path/to/file.py:10:5: E501 Line too long (92 > 88 characters)
```

Parse as: `<file>:<line>:<col>: <rule> <message>`

Ruff exits non-zero if violations are found. Exit 0 means clean.

## Return Format

Return a list of violations:
```
File: path/to/file.py
Line 10, Col 5: E501 — Line too long (92 > 88 characters)
```

If clean, return: "ruff: no violations found"

## Fix Mode

In fix mode (`task python:ruff:fix`), ruff auto-fixes what it can. Some violations
require manual fixes (complex logic issues). After fixing, always re-run check mode
to confirm the file is clean.
```

**Step 2: Verify skill is readable**

```bash
cat py-lint-driven/skills/run-ruff/SKILL.md
```

Expected: full skill content displayed.

**Step 3: Commit**

```bash
git add py-lint-driven/skills/run-ruff/
git commit -m "feat: add run-ruff skill"
```

---

## Task 5: run-complexipy Skill

**Files:**
- Create: `py-lint-driven/skills/run-complexipy/SKILL.md`

**Step 1: Create the skill**

```markdown
---
name: run-complexipy
description: Run complexipy to measure cognitive complexity of Python functions. Exits non-zero when any function exceeds the configured threshold. Returns a list of offending functions with name, file, line, and score.
triggers:
  - "run complexipy"
  - "check cognitive complexity"
  - "complexity violations"
---

# run-complexipy Skill

Run complexipy to detect functions with excessive cognitive complexity.

## How to Use

Call `task python:complexity FILES=<path>` (or `task python:complexity` for whole project).

The threshold is configured via MAX_COGNITIVE_COMPLEXITY in taskfiles/Taskfile.python.yaml
(default: 15). Read the current value from py-lint-driven.local.md (max_cognitive_complexity).

## Key Fact: Exit Codes

complexipy exits non-zero when ANY function exceeds the threshold. This means task chains
(like `task python:lint`) will stop and fail when complexity violations are found.

## Parsing Output

complexipy outputs violations like:
```
src/module.py - process_data has a cognitive complexity of 18 (threshold: 15)
```

## Return Format

```
Complexity violations (threshold: 15):
- process_data in src/module.py — score: 18 (exceeds by 3)
- validate_input in src/utils.py — score: 22 (exceeds by 7)
```

If clean, return: "complexipy: all functions within threshold"

## Fixing Complexity Violations

Complexity violations cannot be auto-fixed. The lint-iterator agent must propose a
refactor strategy and get user confirmation before restructuring logic.
```

**Step 2: Commit**

```bash
git add py-lint-driven/skills/run-complexipy/
git commit -m "feat: add run-complexipy skill"
```

---

## Task 6: run-radon Skill

**Files:**
- Create: `py-lint-driven/skills/run-radon/SKILL.md`

**Step 1: Create the skill**

**IMPORTANT:** radon always exits 0 regardless of results. This skill MUST parse stdout
to determine pass/fail. Do not rely on exit codes.

```markdown
---
name: run-radon
description: Run radon to measure cyclomatic complexity (CC) and maintainability index (MI) of Python files. Parses stdout to enforce thresholds since radon always exits 0. Returns offending functions and files.
triggers:
  - "run radon"
  - "check maintainability"
  - "cyclomatic complexity"
  - "maintainability index"
---

# run-radon Skill

Run radon to check cyclomatic complexity (CC) and maintainability index (MI).

## CRITICAL: radon Always Exits 0

Unlike ruff and complexipy, radon NEVER exits non-zero — even when thresholds are
violated. You MUST parse stdout to determine whether violations exist.

## How to Use

Call `task python:maintainability FILES=<path>`.

This runs two commands:
1. `radon cc -n <RADON_CC_MIN_GRADE> <path>` — shows functions at or below the grade
2. `radon mi <path>` — shows MI scores for all files

Read thresholds from py-lint-driven.local.md:
- `radon_cc_min_grade` — grade cutoff (A=best, F=worst). Default: C
- `radon_mi_floor` — MI floor (0-100). Below this is a violation. Default: 20

## Parsing radon cc Output

radon cc outputs functions in format:
```
src/module.py
    F process_data:10 - D (25)
    M validate:45 - B (8)
```

Grade letters: A (1-5), B (6-10), C (11-15), D (16-20), E (21-25), F (26+)
A function is a violation if its grade is WORSE than radon_cc_min_grade.
"Worse" means later in the alphabet: C is worse than A, D is worse than C.

## Parsing radon mi Output

radon mi outputs:
```
src/module.py - A (67.42)
src/utils.py - C (12.38)
```

Grade: A (>= 20 MI score), B (10-19), C (< 10)
A file is a violation if its MI score is BELOW radon_mi_floor.

## Return Format

```
Radon violations:

Cyclomatic Complexity (threshold: C or better):
- process_data in src/module.py — grade D, score 25

Maintainability Index (floor: 20):
- src/utils.py — MI 12.38 (below floor of 20)
```

If clean, return: "radon: all functions and files within thresholds"
```

**Step 2: Commit**

```bash
git add py-lint-driven/skills/run-radon/
git commit -m "feat: add run-radon skill with stdout parsing logic"
```

---

## Task 7: run-pytest Skill

**Files:**
- Create: `py-lint-driven/skills/run-pytest/SKILL.md`

**Step 1: Create the skill**

```markdown
---
name: run-pytest
description: Run pytest on the tests/ directory or a specific test file. Returns structured pass/fail results. Distinguishes collection errors (broken imports) from assertion failures.
triggers:
  - "run pytest"
  - "run tests"
  - "check tests"
---

# run-pytest Skill

Run pytest and return structured test results.

## How to Use

Full suite: `task python:test`
Single file: `task python:test:file FILE=tests/test_module.py`

pytest exits non-zero on any failure or collection error.

## Two Types of Failures — Handle Differently

**Collection errors** (broken imports, syntax errors in test files):
```
ERROR collecting tests/test_module.py
ImportError: cannot import name 'process' from 'module'
```
→ Surface immediately to the user. Do NOT iterate on collection errors —
  the test file itself is broken and needs a fix before anything else.

**Assertion failures** (tests run but fail):
```
FAILED tests/test_module.py::test_process - AssertionError: assert 0 == 1
```
→ These are expected in TDD red state. Iterate on these by fixing implementation.

## Return Format

On failure:
```
Tests: 2 passed, 1 failed

FAILED: tests/test_module.py::test_process
  AssertionError: assert result == expected
  File: tests/test_module.py, line 12
```

On pass:
```
Tests: 3 passed, 0 failed ✓
```

On collection error:
```
COLLECTION ERROR — tests cannot run:
  tests/test_module.py: ImportError: cannot import name 'process'
  Fix the import before iterating.
```
```

**Step 2: Commit**

```bash
git add py-lint-driven/skills/run-pytest/
git commit -m "feat: add run-pytest skill"
```

---

## Task 8: write-tests Skill

**Files:**
- Create: `py-lint-driven/skills/write-tests/SKILL.md`

**Step 1: Create the skill**

```markdown
---
name: write-tests
description: Given a source file path or function description, generate a failing pytest test file in tests/. Tests are written before implementation (TDD red state). Confirms tests fail after writing.
triggers:
  - "write tests"
  - "generate tests"
  - "create test file"
---

# write-tests Skill

Generate a failing pytest test file for a given source file or description.

## Rules

1. Write tests BEFORE looking at implementation (if implementation exists, ignore it)
2. Tests must fail initially — this is the TDD red state
3. One test per logical behavior, not one per line of code
4. Test file goes in `tests/test_<module_name>.py`

## What to Generate

For each public function/class in the source:
- One happy path test (normal inputs, expected output)
- One or two edge case tests (empty input, boundary values, None, etc.)
- Base tests on type annotations and docstrings — not on implementation

## Test File Structure

```python
import pytest
from <module> import <function>


def test_<function>_happy_path():
    result = <function>(<normal_input>)
    assert result == <expected_output>


def test_<function>_edge_case():
    result = <function>(<edge_input>)
    assert result == <edge_expected>


def test_<function>_invalid_input():
    with pytest.raises(<ExceptionType>):
        <function>(<invalid_input>)
```

## After Writing

Run `task python:test:file FILE=tests/test_<module>.py` to confirm tests fail.

Expected output: FAILED (one or more tests)

If tests PASS before implementation exists, the tests are wrong — revise them.

## Return

Confirm the red state:
```
Tests written: tests/test_module.py
Red state confirmed: 3 tests, 0 passed, 3 failed ✓
```
```

**Step 2: Commit**

```bash
git add py-lint-driven/skills/write-tests/
git commit -m "feat: add write-tests skill"
```

---

## Task 9: report-quality Skill

**Files:**
- Create: `py-lint-driven/skills/report-quality/SKILL.md`

**Step 1: Create the skill**

```markdown
---
name: report-quality
description: Aggregate output from ruff, complexipy, radon, and pytest into a single quality summary with pass/fail verdict against configured thresholds.
triggers:
  - "quality report"
  - "summarize quality"
  - "report results"
---

# report-quality Skill

Aggregate all tool output into a single quality summary.

## What to Include

```
## Quality Report — <path> — <timestamp>

### Tests
<N> passed, <N> failed

### Ruff
<N> violations  |  or: clean ✓

### Cognitive Complexity (complexipy, threshold: <N>)
<N> violations  |  or: clean ✓
Top offenders:
  - <function> in <file> — score <N>

### Cyclomatic Complexity (radon cc, threshold: <grade>)
<N> violations  |  or: clean ✓

### Maintainability Index (radon mi, floor: <N>)
<N> files below floor  |  or: clean ✓
Worst:
  - <file> — MI <score>

### Verdict
PASS ✓  |  FAIL ✗ — <N> issues remain
```

## Verdict Rules

- PASS: all tests green AND ruff clean AND complexipy clean AND radon within thresholds
- FAIL: any of the above is violated

## Saving Reports

For `/quality-report` command only, save the report to:
`.claude/py-lint-driven/reports/YYYY-MM-DD-HH-MM-<path>.md`

For other commands, display inline only.
```

**Step 2: Commit**

```bash
git add py-lint-driven/skills/report-quality/
git commit -m "feat: add report-quality skill"
```

---

## Task 10: iterate-until-clean Skill

**Files:**
- Create: `py-lint-driven/skills/iterate-until-clean/SKILL.md`

**Step 1: Create the skill**

```markdown
---
name: iterate-until-clean
description: Core quality loop. Runs fix pass then verify pass, repeating until tests pass and linting is clean, or iteration_limit is hit. Always uses full lint — never the fast variant. Used by /tdd and /lint-fix commands.
triggers:
  - "iterate until clean"
  - "fix and verify loop"
  - "clean the code"
---

# iterate-until-clean Skill

The core fix/verify loop. Keeps trying until everything is green or the limit is hit.

## IMPORTANT: Always Full Lint

This skill always runs full lint (ruff + complexipy + radon). Never the fast variant.
The fast/full distinction (hooks_run_complexity) applies to hooks only, not this skill.

## Loop Structure

Read `iteration_limit` from py-lint-driven.local.md (default: 5).

For each iteration (starting at 1):

### Fix Pass
Run `task python:tdd:fix`
This runs: test → ruff:fix → complexity check → maintainability check → fmt

The lint-iterator agent handles failures:
- Test failures → agent fixes implementation logic
- ruff violations remaining after auto-fix → agent fixes manually
- Complexity violations → agent proposes refactor, waits for user confirmation
- Maintainability violations → agent flags, proposes simplification

### Verify Pass
Run `task python:tdd`
This runs: test → full lint (read-only, no fixes)

If verify pass is fully green → done, report success.
If still dirty → increment iteration counter, repeat.

## On Limit Hit

Do NOT silently fail. Report clearly:
```
Iteration limit (5) reached. Remaining issues:
- Tests: 1 still failing — tests/test_module.py::test_edge_case
- Ruff: 2 violations — src/module.py:45, src/utils.py:12
- Complexity: 1 function still above threshold — process_data (score: 18)

Action required: fix these manually before proceeding.
```

## Return on Success

```
Clean after <N> iteration(s):
- Tests fixed: <N>
- Ruff violations fixed: <N>
- Format issues fixed: <N>
All checks passing ✓
```
```

**Step 2: Commit**

```bash
git add py-lint-driven/skills/iterate-until-clean/
git commit -m "feat: add iterate-until-clean skill"
```

---

## Task 11: lint-iterator Agent

**Files:**
- Create: `py-lint-driven/agents/lint-iterator.md`

**Step 1: Create the agent**

```markdown
---
name: lint-iterator
description: Fixes test failures and lint violations during the iterate-until-clean loop. Tests run first — implementation is fixed before lint violations are touched. Never modifies test files. Complexity refactors require user confirmation.
---

# lint-iterator Agent

You are the lint-iterator agent. Your job is to fix test failures and lint violations
during one iteration of the fix/verify loop.

## Rules

### Rule 1: Tests First, Always
Run `task python:test` first. If tests fail, fix the implementation before touching
any lint violations. Lint is irrelevant on broken code.

### Rule 2: Three Failure Types — Handle Each Differently

**Collection errors** (import errors, syntax errors in test files):
- Surface immediately to the user
- Do NOT attempt to fix — the test file is broken and the user must intervene
- Stop the iteration

**Assertion failures** (tests run but fail):
- Fix the implementation in the source file
- Never modify the test file — tests are the spec

**Lint violations** (after tests pass):
- ruff violations: apply auto-fix first (`task python:ruff:fix`), then fix remaining manually
- Complexity violations: propose a refactor strategy, WAIT for user confirmation before applying
- Maintainability violations: flag and suggest simplification options

### Rule 3: Never Touch Test Files
You are scoped to Write and Edit on source files only.
Files in `tests/` are immutable — they define the spec.

### Rule 4: Group Related Fixes
Do not fix one violation at a time. Group all violations of the same type in the
same file and fix them together. E.g., fix all E501 violations in module.py at once.

### Rule 5: Report on Completion
After each iteration, report:
- What was fixed (tests, ruff, complexity, maintainability)
- What remains (if anything)
- How many iterations remain before the limit
```

**Step 2: Commit**

```bash
git add py-lint-driven/agents/lint-iterator.md
git commit -m "feat: add lint-iterator agent"
```

---

## Task 12: /tdd Command

**Files:**
- Create: `py-lint-driven/commands/tdd.md`

**Step 1: Create the command**

```markdown
---
name: tdd
description: Test-driven development entry point. Write a description of what to build — Claude writes failing tests first, then implementation, then iterates until both tests pass and linting is clean.
argument-hint: "<description of what to build>"
---

# /tdd Command

Given a description of what to build, run the full TDD cycle.

## Arguments

`$ARGUMENTS` — natural language description of the feature to build
Example: `/tdd a function that validates email addresses`

## Flow

1. **Determine file names**
   From `$ARGUMENTS`, determine:
   - Source file: `src/<module>.py` (or appropriate path)
   - Test file: `tests/test_<module>.py`

2. **Write failing tests** (red state)
   Use the `write-tests` skill to generate `tests/test_<module>.py`.
   Run `task python:test:file FILE=tests/test_<module>.py` to confirm tests fail.
   If tests pass before implementation exists — the tests are wrong, revise them.

3. **Write implementation**
   Write `src/<module>.py` with the minimal implementation to satisfy the tests.
   Do not over-engineer — write only what the tests require (YAGNI).

4. **Iterate until clean**
   Use the `iterate-until-clean` skill.
   It runs fix pass (`task python:tdd:fix`) then verify pass (`task python:tdd`),
   repeating up to `iteration_limit` times until everything is green.

5. **Report**
   Use the `report-quality` skill to display the final quality summary.
```

**Step 2: Commit**

```bash
git add py-lint-driven/commands/tdd.md
git commit -m "feat: add /tdd command"
```

---

## Task 13: /tdd-test Command

**Files:**
- Create: `py-lint-driven/commands/tdd-test.md`

**Step 1: Create the command**

```markdown
---
name: tdd-test
description: Generate tests for an existing source file without writing implementation. Useful for adding test coverage to legacy code. Shows failing state and stops.
argument-hint: "<path to source file>"
---

# /tdd-test Command

Generate tests for an existing source file. Does not write implementation.

## Arguments

`$ARGUMENTS` — path to the source file to generate tests for
Example: `/tdd-test src/utils.py`

## Flow

1. **Read the source file** at `$ARGUMENTS`
   Identify all public functions and classes.

2. **Write failing tests**
   Use the `write-tests` skill to generate `tests/test_<module>.py`.
   Write tests based on function signatures, type annotations, and docstrings.
   Do NOT look at the implementation body — treat it as a black box.

3. **Confirm red state**
   Run `task python:test:file FILE=tests/test_<module>.py`.
   Expected: tests fail (the implementation may exist but tests are written
   against the spec, not the current behavior).

4. **Stop**
   Do not write or modify any implementation.
   Inform the user: "Tests written. Run `/lint-fix` to iterate until clean,
   or fix the implementation manually."
```

**Step 2: Commit**

```bash
git add py-lint-driven/commands/tdd-test.md
git commit -m "feat: add /tdd-test command"
```

---

## Task 14: /lint-fix and /lint-check Commands

**Files:**
- Create: `py-lint-driven/commands/lint-fix.md`
- Create: `py-lint-driven/commands/lint-check.md`

**Step 1: Create lint-fix.md**

```markdown
---
name: lint-fix
description: Fix all linting and test issues in the project or a specific path. Runs the full fix/verify loop until clean or iteration limit hit.
argument-hint: "[path — optional, defaults to current directory]"
---

# /lint-fix Command

Run the unified fix/verify quality loop on existing code.

## Arguments

`$ARGUMENTS` — optional path (file or directory). Defaults to `.` (whole project).

## Flow

1. **Iterate until clean**
   Use the `iterate-until-clean` skill on `$ARGUMENTS` (or `.` if not provided).
   The skill runs fix pass (`task python:tdd:fix`) then verify pass (`task python:tdd`),
   repeating up to `iteration_limit` times.

2. **Report**
   Use the `report-quality` skill to display the final quality summary.
```

**Step 2: Create lint-check.md**

```markdown
---
name: lint-check
description: Read-only quality audit. Runs tests and full linting with no fixes applied. Safe to run before a pull request. Displays a quality report.
argument-hint: "[path — optional, defaults to current directory]"
---

# /lint-check Command

Read-only audit — no files are modified.

## Arguments

`$ARGUMENTS` — optional path. Defaults to `.`.

## Flow

1. **Run full TDD check (read-only)**
   Run `task python:tdd` — this runs tests and full lint without fixing anything.

2. **Also check Taskfile setup**
   If `Taskfile.yaml` or `taskfiles/Taskfile.python.yaml` do not exist, generate
   them from `py-lint-driven/templates/` now.
   If `py-lint-driven.local.md` does not exist in the project root, copy the
   template from `py-lint-driven/templates/py-lint-driven.local.md`.

3. **Report**
   Use the `report-quality` skill to display results.
   Report is read-only — clearly state that no fixes were applied.
```

**Step 3: Commit**

```bash
git add py-lint-driven/commands/lint-fix.md py-lint-driven/commands/lint-check.md
git commit -m "feat: add /lint-fix and /lint-check commands"
```

---

## Task 15: /quality-report Command

**Files:**
- Create: `py-lint-driven/commands/quality-report.md`

**Step 1: Create the command**

```markdown
---
name: quality-report
description: Deep complexity and maintainability analysis. Runs complexipy and radon only (no ruff, no tests). Highlights the 5 most complex functions and files with poor maintainability. Saves a timestamped report.
argument-hint: "[path — optional, defaults to current directory]"
---

# /quality-report Command

Deep quality analysis focused on complexity and maintainability metrics.

## Arguments

`$ARGUMENTS` — optional path. Defaults to `.`.

## Flow

1. **Run complexity check**
   Call `run-complexipy` skill: `task python:complexity FILES=$ARGUMENTS`

2. **Run maintainability check**
   Call `run-radon` skill: `task python:maintainability FILES=$ARGUMENTS`
   Parse stdout to detect violations (radon always exits 0).

3. **Build deep report**
   Use the `report-quality` skill. Include:
   - Top 5 most complex functions (highest complexipy score)
   - Top 5 worst MI files (lowest radon mi score)
   - Full list of CC violations
   - Full list of MI violations

4. **Save report**
   Save to `.claude/py-lint-driven/reports/YYYY-MM-DD-HH-MM-<sanitized-path>.md`
   Create the directory if it doesn't exist.
   Inform the user where the report was saved.
```

**Step 2: Commit**

```bash
git add py-lint-driven/commands/quality-report.md
git commit -m "feat: add /quality-report command"
```

---

## Task 16: Hooks

**Files:**
- Create: `py-lint-driven/hooks/post-write-quality.json`
- Create: `py-lint-driven/hooks/post-edit-quality.json`

**Background:** Claude Code hooks fire on events and run either a bash command or a prompt.
PostToolUse hooks fire after a tool (Write, Edit, etc.) completes. The hook receives
the tool input (including the file path written).

**Step 1: Create post-write-quality.json**

```json
{
  "name": "post-write-quality",
  "event": "PostToolUse",
  "tool": "Write",
  "description": "After writing a .py file, run quality checks. Test files get red-state confirmation + ruff only. Source files get the full hook quality loop.",
  "prompt": "A Python file was just written: {{tool.input.file_path}}\n\nRead py-lint-driven.local.md for config. If hooks_enabled is false, stop immediately.\n\nDetermine file type:\n- If the path contains '/tests/' or starts with 'tests/': this is a TEST FILE\n- Otherwise: this is a SOURCE FILE\n\nFor TEST FILES:\n1. Run: task python:test:file FILE={{tool.input.file_path}}\n2. If tests FAIL — this is correct (red state). Report: 'Red state confirmed ✓'\n3. If tests PASS before implementation exists — warn the user: 'Tests pass unexpectedly — verify they are testing the right behavior'\n4. Run: task python:ruff FILES={{tool.input.file_path}}\n5. If ruff violations found, fix them and re-run.\n\nFor SOURCE FILES:\n1. Check config: hooks_run_complexity and tdd_enabled\n2. Select task from this table:\n   - hooks_run_complexity=false, tdd_enabled=true  → task python:tdd:fast\n   - hooks_run_complexity=true,  tdd_enabled=true  → task python:tdd\n   - hooks_run_complexity=false, tdd_enabled=false → task python:lint:fast\n   - hooks_run_complexity=true,  tdd_enabled=false → task python:lint\n3. Run the selected task on {{tool.input.file_path}}\n4. If clean → done, no output needed\n5. If violations found → use the lint-iterator agent to fix them\n6. Re-run the selected task to verify\n7. If still dirty after iteration_limit attempts → report what remains to the user"
}
```

**Step 2: Create post-edit-quality.json**

```json
{
  "name": "post-edit-quality",
  "event": "PostToolUse",
  "tool": "Edit",
  "description": "After editing a .py file, run quality checks. Identical logic to post-write-quality but scoped to the edited file.",
  "prompt": "A Python file was just edited: {{tool.input.file_path}}\n\nRead py-lint-driven.local.md for config. If hooks_enabled is false, stop immediately.\n\nDetermine file type:\n- If the path contains '/tests/' or starts with 'tests/': this is a TEST FILE\n- Otherwise: this is a SOURCE FILE\n\nFor TEST FILES:\n1. Run: task python:test:file FILE={{tool.input.file_path}}\n2. Run: task python:ruff FILES={{tool.input.file_path}}\n3. Fix any ruff violations.\n\nFor SOURCE FILES:\n1. Check config: hooks_run_complexity and tdd_enabled\n2. Select task from this table:\n   - hooks_run_complexity=false, tdd_enabled=true  → task python:tdd:fast\n   - hooks_run_complexity=true,  tdd_enabled=true  → task python:tdd\n   - hooks_run_complexity=false, tdd_enabled=false → task python:lint:fast\n   - hooks_run_complexity=true,  tdd_enabled=false → task python:lint\n3. Run the selected task on {{tool.input.file_path}}\n4. If clean → done\n5. If violations → lint-iterator agent → re-verify → report if limit hit"
}
```

**Step 3: Commit**

```bash
git add py-lint-driven/hooks/
git commit -m "feat: add post-write-quality and post-edit-quality hooks"
```

---

## Task 17: End-to-End Test

Manually verify the full plugin works together.

**Step 1: Set up a test project**

```bash
mkdir -p /tmp/e2e-test/src /tmp/e2e-test/tests
cd /tmp/e2e-test

# Copy Taskfile templates
cp <plugin-path>/py-lint-driven/templates/Taskfile.yaml .
mkdir -p taskfiles
cp <plugin-path>/py-lint-driven/templates/Taskfile.python.yaml taskfiles/
cp <plugin-path>/py-lint-driven/templates/py-lint-driven.local.md .

# Install the plugin in Claude Code
# (follow Claude Code marketplace install instructions)
```

**Step 2: Test /tdd**

In Claude, run:
```
/tdd a function that calculates the factorial of a non-negative integer
```

Verify:
- `tests/test_factorial.py` is created with failing tests
- `src/factorial.py` is written
- iterate-until-clean runs
- Final report shows all green

**Step 3: Test hooks**

Write a dirty Python file via Claude:
```
Write a Python file src/dirty.py with a function that has 5+ levels of nesting
```

Verify:
- Hook fires automatically
- lint-iterator attempts to fix
- Result is cleaner code or a clear report of what remains

**Step 4: Test /lint-check**

```
/lint-check
```

Verify:
- No files modified
- Report shows current state

**Step 5: Test task python:ci locally**

```bash
cd /tmp/e2e-test
git init && git add . && git commit -m "init"
# Make a change to a .py file
echo "# change" >> src/factorial.py
git add src/factorial.py

# Simulate CI (without GITHUB_BASE_REF, falls back to main)
task python:ci
```

Verify: runs ruff + complexipy + radon on the changed file only, runs full pytest.

**Step 6: Commit final**

```bash
cd <plugin-root>
git add .
git commit -m "feat: py-lint-driven plugin complete"
```

---

## Summary

| Task | What it builds |
|---|---|
| 1 | Plugin scaffold and plugin.json |
| 2 | Taskfile templates |
| 3 | Configuration template |
| 4 | run-ruff skill |
| 5 | run-complexipy skill |
| 6 | run-radon skill (with stdout parsing) |
| 7 | run-pytest skill |
| 8 | write-tests skill |
| 9 | report-quality skill |
| 10 | iterate-until-clean skill |
| 11 | lint-iterator agent |
| 12 | /tdd command |
| 13 | /tdd-test command |
| 14 | /lint-fix and /lint-check commands |
| 15 | /quality-report command |
| 16 | post-write-quality and post-edit-quality hooks |
| 17 | End-to-end verification |
